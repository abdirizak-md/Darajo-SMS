# Darajo SMS - Backend

Simple Node.js + MongoDB backend scaffold for the Darajo SMS frontend.

Quick start:

1. Copy `.env.example` to `.env` and set `MONGO_URI`.
2. Install dependencies:

   `cd server && npm install express mongoose dotenv cors morgan`
   `npm install --save-dev nodemon`

3. Run in development:

   `npm run dev`
